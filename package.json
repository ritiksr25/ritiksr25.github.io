{
  "name": "ritiksr25-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "ghpdeploy": "npm run build && touch out/.nojekyll && gh-pages -t -b master -d out"
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "9.4.0",
    "react": "16.13.1",
    "react-bootstrap": "^1.0.1",
    "react-dom": "16.13.1"
  }
}
